repos:
  - repo: local
    hooks:
      - id: terraform-fmt
        name: Terraform Format
        language: system
        entry: terraform fmt -recursive
        files: \.tf$
        exclude: \.terraform\/.*$